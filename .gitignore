node_modules/
.next/
out/
.env.local
*.tsbuildinfo
.DS_Store

# bv (beads viewer) local config and caches
.bv/

# Commercially sensitive features (local-only, not pushed to public repo)
src/app/api/monetization/
src/app/monetization/
src/hooks/useMonetization.ts

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Pre-Dolt beads backup snapshot (one-off, not active)
.beads-backup-pre-dolt/

# Local env backups
.env.local.bak-*
