# Task: beads_web Feature Improvements

Work through these ready beads in priority order. Commit after each.

## Ready Beads (from `bd ready`)

1. **bw-gop (P2)**: Auto-compute complexity score when creating beads to improve release ETA accuracy
2. **bw-1o9 (P3)**: Fleet card: show research report preview snippet on hover or inline
3. **bw-27s (P3)**: Fleet page: add watch directory for auto-discovery of new app repos
4. **bw-91h (P4)**: Display story points in issue table and detail page
5. **bw-rx3 (P4)**: Add tokens-per-story-point efficiency panel

## Workflow
- `bd show <id>` for details on each bead
- `bd update <id> --status=in_progress` before starting
- `bd close <id>` after completing and committing
- `bd sync --flush-only` when done

When completely finished, run: openclaw system event --text "Done: Pip completed beads_web improvements" --mode now
